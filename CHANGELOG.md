# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Stall detection tests for Phase 5 path — covers auto loop-back scenario that skips Phase 6 (#62)
- `stall_detected`, `baseline_global_iter`, and `features_skip` fields in test helper `create_state_file` (#63)
- `plans/` directory in test `setup_test_dir` to mirror real initialized state (#64)
- Explicit Phase 4 declaration in hypothesis-generator agent prompt (#65)
- Test suite expanded from 77 to 79 tests (#66)
- Concurrency guard via flock in stop-hook.sh — prevents two simultaneous hook instances from corrupting state (#39)
- State field validation after reading — resets invalid current_phase and global_iteration with warning (#40)
- Diagnostic warning when transcript parsing fails but hook input was received (#41)
- .gitignore to prevent accidental commit of review-output/ and sota-output/ (#42)
- `features_skip: 0` field in setup-sota-loop.sh initial state file (#43)
- SECURITY INVARIANT documentation comment on eval in probe-runner.sh (#44)
- Trap-based cleanup for TEMP_INIT_DIR in probe_e2e (#45)
- Warning when loop-back is requested but max refinement cycles reached (#46)
- Skip tracking across the entire loop — `features_skip` field in state file, baselines, progress history, and system message (#38)
- SOTA completion blocker when E2E probes are skipped — loop cannot declare SOTA while skip > 0 (#38)
- Auto loop-back when features_failing=0 but features_skip > 0 — prevents false "all passing" completion (#38)
- `skip=N` support in `FEATURES_STATUS` marker for accurate skip count propagation (#38)
- E2E validation dimension in quality-evaluator Phase 1 rubric — scores 0.0 if all E2E probes skipped (#38)
- Skipped features section in report-writer output with remediation table (#38)
- 14 new tests for skip tracking (marker detection, state reading, SOTA blocking, auto loop-back, system message, baselines) (#38)
- Test suite expanded from 63 to 77 tests (#38)
- Budget enforcement in stop-hook.sh — loop now stops when spent_usd >= budget_usd (#22)
- Tests for budget enforcement (3 cases: exhausted, remaining, unlimited) (#23)
- Tests for baseline HEAD rollback (validates ref and restores file state) (#24)
- Test suite expanded from 56 to 61 tests (#25)
- Tests for Phase 5 quality gate behavior (advance on pass, repeat on fail) (#28)
- Test suite expanded from 61 to 63 tests (#28)
- CHANGELOG.md following Keep a Changelog format (#11)
- plans/ directory in README output structure documentation (#12)
- `baseline_global_iter` field in state file for deterministic rollback tracking (#26)
- `LOOP_BACK_TO_PROBE` marker documentation in report-writer agent (#31)

### Fixed
- README listing hypothesis-generator and implementation-coder as Phase 3 — corrected to Phase 4 (evolve) (#74)
- python3 not listed as prerequisite in README despite being required by stop-hook and probe-runner (#75)
- "6-phase pipeline" in marketplace.json — corrected to "7-phase pipeline" matching actual phases 0-6 (#67)
- Dead branch in probe_cli `CLI_BIN` fallback — simplified to use outer `THEO_BIN` with early return (#68)
- Redundant explicit `rm -rf` alongside `trap RETURN` in probe_e2e — now relies solely on trap (#69)
- `--max-cycles`, `--max-iterations`, `--budget` not validated as integers in setup-sota-loop.sh (#70)
- `--completion-done` with double quotes silently breaking YAML frontmatter — now rejects invalid characters (#71)
- `sota-status.md` sed frontmatter extraction — replaced with awk for correct parsing (#72)
- Duplicate `### Added`, `### Fixed`, `### Changed` sections in CHANGELOG `[Unreleased]` — merged into single sections (#73)
- CRITICAL: Stall detection structurally blind — checked phase 6 entries but auto loop-back skips phase 6; now checks phase 5 OR 6 with phase_complete flag (#47)
- CRITICAL: State file write not atomic — mktemp created in /tmp (different filesystem); now creates in same directory as state file with trap cleanup (#48)
- Quality gate default-pass on missing marker — changed logic from negative check (!=0) to positive check (==1); absent marker now repeats phase (#49)
- Prompt body truncation on --- lines — replaced sed range pattern with awk that correctly extracts content after second --- (#50)
- Unquoted heredoc allowing shell expansion in prompt body — replaced with printf '%s' for literal content (#51)
- Baseline save failure silently continuing — now retries with fallback and emits CRITICAL diagnostic (#52)
- FEATURES_FAILING integer comparison crash on empty value — added 2>/dev/null guard consistent with FEATURES_SKIP (#53)
- `color` variable unbound in probe-runner.sh write_result — declared local with NC default + added * case branch (#54)
- Quality gate probes (check_unwrap, check_unsafe) always passing due to || true — removed || true to allow real failure detection (#55)
- probe_cli using hardcoded debug binary instead of discovered release binary — now uses outer THEO_BIN (#56)
- Stale comment "phases 2-4" removed (was replaced by rewritten quality gate logic) (#57)
- Malformed JSONL line in progress history now skipped instead of aborting all stall detection parsing (#58)
- DISCARD rollback reading wrong baseline iteration — used current `global_iteration` instead of saved baseline reference, causing rollback to silently fail (#26)
- Shell command injection via `--completion-done` argument — unquoted heredoc in setup script allowed arbitrary command execution (#27)
- `passing=` and `failing=` regex not anchored to `FEATURES_STATUS` marker — could capture wrong values from agent output (#29)
- Test count in README and help.md showing 53 instead of actual count (#30)
- "6-Phase Protocol" heading documenting 7 phases (0–6) — renamed to "7-Phase Protocol" (#32)
- Duplicated step number 2 in hypothesis-generator agent, causing all subsequent steps to be off by one (#33)
- Phase 5 quality gate not covered by test suite — test logic used range 2-4 instead of 2-5 (#28)
- `memory.infra_crate_tests` probe matching `"test result"` instead of `"test result: ok"` — false positive on failing tests (#34)
- Unquoted `$TEMP_INIT_DIR` in E2E init probe — could break on paths with spaces (#35)
- Unused `state/` subdirectory created by setup script but never referenced (#36)
- Shell injection vulnerabilities in probe-runner.sh — values now passed via sys.argv instead of string interpolation (#1)
- Shell injection in stop-hook.sh Python blocks — values now passed via os.environ instead of string interpolation (#2)
- DISCARD rollback destroying unrelated files — git clean now excludes sota-output/ and .claude/ (#3)
- Baseline stash mechanism was no-op (push then immediate pop) — replaced with HEAD ref tracking (#4)
- Phase numbering inconsistency between code (0-6) and documentation (0-5 with "Phase 2.5") — unified to 0-6 throughout (#5)
- System message showing "Phase N/5" instead of "Phase N/6" (#6)
- sota-status truncating frontmatter via head -25 — now reads full frontmatter with sed (#7)
- report-writer hardcoding "132 features" instead of reading from registry (#8)
- Shell injection in setup-sota-loop.sh — REGISTRY_PATH now passed via os.environ instead of string interpolation (#13)
- DISCARD rollback ignoring saved baseline HEAD — now uses `git checkout <baseline_ref> -- .` instead of always resetting to current HEAD (#14)
- PHASE_NAMES comments showing wrong phase numbers for evolve (said Phase 3, is Phase 4) and verify (said Phase 4, is Phase 5) (#15)
- stop-hook.sh header comment referencing "git stash" instead of "git checkout" after stash removal (#16)
- sota-status.md sed command capturing only first `---` instead of full frontmatter block (#17)
- sota-prompt.md referencing phases 0-5 instead of 0-6 in "Read State First" section (#18)
- sota-prompt.md documenting quality gates for phases 2-4 instead of 2-5 (verify phase was missing) (#19)
- help.md showing hypothesis-generator in Phase 3-4 instead of Phase 4 only (#20)
- test-hook-logic.sh test title saying "covers 0-5" instead of "covers 0-6" (#21)

### Changed
- README and help.md test count updated from 77 to 79 (#66)
- README test count updated from 63 to 77 (#59)
- help.md test count updated from 63 to 77 (#60)
- README quality_threshold removed from config table (parameter was never implemented; threshold lives in agent rubric) (#61)
- Phase 0 rubric in quality-evaluator marked as informational (no quality gate enforced by hook) (#37)

### Removed
- Dead code: unused write_json helper function in stop-hook.sh (#9)
- Legacy phase markers (PHASE_3_COMPLETE for evolve, PHASE_5_COMPLETE for report) — kept only PHASE_2_5_COMPLETE for backward compatibility (#10)

[Unreleased]: https://github.com/paulohenriquevn/sota-evolution-loop/compare/v1.0.0...HEAD
